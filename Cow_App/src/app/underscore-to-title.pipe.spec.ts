import { UnderscoreToTitlePipe } from './underscore-to-title.pipe';

describe('UnderscoreToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new UnderscoreToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
